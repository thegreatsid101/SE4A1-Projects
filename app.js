const prompt = require("prompt-sync")({ sigint: true });

console.log("Calculator written in Node.js\n\n\n");

const firstNum = prompt("Enter first number: ");
const secondNum = prompt("Enter second number: ");

